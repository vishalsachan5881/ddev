# -*-perl-*-

package.Harmony-sachanv-first-app = {
    interfaces = (1.0);

    deploy = {
        generic = true;
    };

    build-environment = {
        chroot = basic;
        network-access = blocked;
    };

    build-system = no-op;
    build-tools = {
        1.0 = {
            NoOpBuild = 1.0;
        };
    };

    dependencies = {
        1.0 = {
        };
    };

    runtime-dependencies = {
        1.0 = {
        };
    };

};
